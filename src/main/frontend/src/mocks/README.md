Not used anymore, but kept in case would be interesting for Zoe.

The `mocks` dir was originally used to test the UI before the BE was implemented.

Could be used as a fake export in the real API files in `/utilities`, meaning we would export the same name of the proper API, but instead of the real methods, it would contain the mock ones.
