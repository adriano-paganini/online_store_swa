This dir contains all the components.

Most of the components are meant to be 'dumb' => no data manipulation, just display and use of propagated functions from the Page (in `\views`).
Not always respected...

`/ui` contains components from ShadCN.
`/routes` contain protection routes for different user roles.
